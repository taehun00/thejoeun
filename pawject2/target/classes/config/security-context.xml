<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">

	<!-- 암호화 -->
	<bean  id="passwordEncoder"   
		   class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"  />
	<!-- 로그인성공시 -->
	<bean  id="successHandler"  class="com.pawject.security.LoginSuccessHandler"/>
	
	<!-- 로그인실패시 -->
	<!-- 로그인실패시 -->
	<bean id="loginFailureHandler" class="com.pawject.security.LoginFailureHandler"/>
	<!-- <bean  id="deninedHandler"  class="com.pawject.security.DeninedHandler"/> -->
	
	<!-- 유저정보    --> 
	<bean  id="customUserDetailsService"  class="com.pawject.security.CustomUserDetailsService"/>
	
	
	<security:http>
		<security:intercept-url  pattern="/security/all"      access="permitAll" />
		<security:intercept-url  pattern="/security/member"   access="hasRole('ROLE_MEMBER')" />
		
	   <!-- 마이페이지와 수정 기능은 인증 필수- session 연결 -->	
		<security:intercept-url pattern="/security/login" 		access="permitAll"/>
		<security:intercept-url pattern="/security/join" 		access="permitAll"/>
		<security:intercept-url pattern="/security/iddouble"			 	access="permitAll"/>
		
		<!-- 관리자 전용 URL -->
	    <security:intercept-url pattern="/security/adminList" access="hasRole('ROLE_ADMIN')" />
	    <security:intercept-url pattern="/security/selectAll" access="hasRole('ROLE_ADMIN')" />
	    <security:intercept-url pattern="/security/select" access="hasRole('ROLE_ADMIN')" />
	    <security:intercept-url pattern="/security/updateAdmin" access="hasRole('ROLE_ADMIN')" />
	    <security:intercept-url pattern="/security/deleteAdmin" access="hasRole('ROLE_ADMIN')" />

		<!-- 일반 사용자 URL -->
	    <security:intercept-url pattern="/security/mypage" access="isAuthenticated()" />
	    <security:intercept-url pattern="/security/update" access="isAuthenticated()" />
	    <security:intercept-url pattern="/security/deleteMember" access="isAuthenticated()" />

		
		
		<security:intercept-url pattern="/security/**" 			access="isAuthenticated()"/>

		<security:form-login  
	    login-page="/security/login"
	    login-processing-url="/security/doLogin"
	    authentication-success-handler-ref="successHandler"
	    authentication-failure-handler-ref="loginFailureHandler"

		/>
		<!-- authentication-failure-url="/security/fail" -->
		<security:logout     
			logout-url="/security/logout"
			logout-success-url="/security/login"
			invalidate-session="true"
		/>
	</security:http>

	<security:authentication-manager>	
		<security:authentication-provider  user-service-ref="customUserDetailsService">
		 	<security:password-encoder  ref="passwordEncoder" />
		</security:authentication-provider>
	</security:authentication-manager>

<!-- 	<security:authentication-manager>	
		<security:authentication-provider>
			<security:user-service>
				<security:user name="member"   password="member" authorities="ROLE_MEMBER"/>
			</security:user-service>
		</security:authentication-provider>
	</security:authentication-manager> -->
	
</beans>
