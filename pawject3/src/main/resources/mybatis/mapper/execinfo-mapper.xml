<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pawject.dao.exec.ExecinfoDao">
 <resultMap id="execimfoMap" type="ExecinfoDto">
 	<result property="execid"         column="EXECID"/>
 	<result property="exectype"       column="EXECTYPE"/>
 	<result property="description"    column="DESCRIPTION"/>
 	<result property="avgkcal30min"   column="AVGKCAL30MIN"/>
 	<result property="exectargetmin"  column="EXECTARGETMIN"/>
 	<result property="suitablefor"    column="SUITABLEFOR"/>
 	<result property="intensitylevel" column="INTENSITYLEVEL"/>
 	<result property="createdAt"      column="CREATEDAT"/>
 	<result property="updatedAt"      column="UPDATEDAT"/>
 </resultMap>
 
 
 <insert id="insertinfo" parameterType=""></insert>
 <update id="updateinfo" parameterType=""></update>
 <delete id="deleteinfo" parameterType=""></delete>
 <select resultMap="execimfoMap"  id="selectAllinfo" ></select>
 <select resultMap="execimfoMap"  id="selectinfo" parameterType="int"></select>

  <!-- 페이징(1) - ### -->
  <select id="###" parameterType="java.util.HashMap" resultMap="###"></select>
  
  <!-- 페이징(2) - ### -->
  <select id="###" resultType="###"></select>
  
  <!-- 페이징(3) - ### -->
  <select id="###" parameterType="java.util.HashMap" resultMap="###"></select>
  
  <!-- 페이징(4) - ### -->
  <select id="###" parameterType="###" resultType="int"></select>

</mapper>

<!-- SQL구문 
insert

update

delect

selectAll

select

PAGING_1

PAGING_2

PAGING_3

PAGING_4


 -->