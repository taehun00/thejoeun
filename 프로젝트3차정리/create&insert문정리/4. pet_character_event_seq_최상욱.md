
AI 캐릭터 생성 이벤트

drop table PETCHARACTEREVENT;

이름        널?       유형            
--------- -------- ------------- 
CHARID    NOT NULL NUMBER        
USERID    NOT NULL NUMBER        
KINDPET   NOT NULL VARCHAR2(100) 
PROMPT             VARCHAR2(500) 
IMAGEURL           VARCHAR2(500) 
CREATEDAT          DATE  


---1 PETCHARACTEREVENT 테이블 재정의
CREATE TABLE PetCharacter (
    charid     NUMBER PRIMARY KEY,          -- 캐릭터 고유 ID
    userid     NUMBER NOT NULL,             -- 생성자 (USERS.userid 참조)
    kindpet    VARCHAR2(100) NOT NULL,      -- 반려동물 종
    prompt     VARCHAR2(500),               -- AI 프롬프트
    imageurl   VARCHAR2(500),               -- 생성된 이미지 URL
    createdat  DATE DEFAULT SYSDATE
);

CREATE SEQUENCE petcharacter_seq;

insert 구문 필요없습니다. 
가능하면 원본사이즈는 512x512 로 업로드해주세요
